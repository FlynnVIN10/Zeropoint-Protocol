# Evidence directory - serve raw files with proper headers
/evidence/* /evidence/:splat 200!

# API routes
/api/* /api/:splat 200

# Status routes
/status/* /status/:splat 200

# Fallback to SPA
/* /index.html 200