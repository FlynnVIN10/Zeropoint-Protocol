# Getting Started

Welcome to Zeropoint Protocol! This guide will help you get up and running with our platform.

## Prerequisites

Before you begin, ensure you have:

- **Node.js 18+** and npm
- **Git** for version control
- **Access to tinybox hardware** (for full functionality)
- **ROCm 5.7+** installed on your system

## Quick Installation

1. **Clone the repository**
   ```bash
   git clone https://github.com/FlynnVIN10/Zeropoint-Protocol.git
   cd Zeropoint-Protocol
   ```

2. **Install dependencies**
   ```bash
   npm install
   ```

3. **Configure environment**
   ```bash
   cp config/production.env .env.local
   # Edit .env.local with your configuration
   ```

4. **Start development server**
   ```bash
   npm run dev
   ```

5. **Open your browser**
   Navigate to `http://localhost:3000`

## First Steps

### 1. Access the Control Center

Once the application is running, navigate to `/control/overview` to see the main dashboard.

### 2. Check System Status

The overview page displays:
- System KPIs and metrics
- Recent deployments
- Active incidents
- Quick action buttons

### 3. Explore Synthiants

Visit `/control/synthiants` to monitor:
- Active AI agents
- Queue statistics
- Chat sessions
- Performance metrics

### 4. Monitor Consensus

Go to `/control/consensus` to track:
- Active proposals
- Voting progress
- Quorum status
- Veto decisions

### 5. View Metrics

Check `/control/metrics` for:
- Performance data
- Cost breakdown
- RAG metrics
- Real-time charts

### 6. Review Audit Trail

Visit `/control/audit` to see:
- System events
- Security incidents
- Governance activities
- Detailed logs

## Configuration

### Environment Variables

Key configuration options in your `.env.local`:

```bash
# Scope Controls
MOCKS_DISABLED=1
NO_SYNTHETIC_DATA=1
REAL_COMPUTE_ONLY=1

# Hardware Attestation
HARDWARE_ATTESTATION_REQUIRED=1
ROCM_VERSION=5.7.0

# Data Governance
PII_SCANNING_ENABLED=1
LICENSE_VALIDATION_ENABLED=1
CHECKSUM_VERIFICATION_ENABLED=1
```

### tinygrad Integration

The platform automatically integrates with tinygrad when:
- ROCm is properly installed
- tinybox hardware is detected
- Environment variables are configured

## Next Steps

- **Read the [Installation Guide](/docs/installation)** for detailed setup
- **Configure [Data Governance](/docs/governance)** for your datasets
- **Set up [Monitoring](/docs/monitoring)** for production use
- **Learn about [Deployment](/docs/deployment)** options

## Troubleshooting

### Common Issues

1. **Mock Detection Failing**
   - Ensure `MOCKS_DISABLED=1` is set
   - Check for mock implementations in your code
   - Review CI/CD configuration

2. **ROCm Not Detected**
   - Verify ROCm installation
   - Check GPU drivers
   - Ensure tinybox hardware is connected

3. **PII Scanning Errors**
   - Verify dataset integrity
   - Check checksums
   - Review license compliance

### Getting Help

- **Documentation**: Browse our comprehensive guides
- **GitHub Issues**: Report bugs and request features
- **Community**: Join discussions and get support

---

*Ready to get started? Let's build something amazing together! 🚀*
