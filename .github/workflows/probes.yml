name: probes
on: [deployment_status]
jobs:
  check:
    if: github.event.deployment_status.state == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Verify JSON probes
        env:
          URL: ${{ github.event.deployment_status.target_url }}
        run: |
          set -euo pipefail
          curl -fsS "$URL/api/healthz" | jq -e '.status=="ok"'
          curl -fsS "$URL/api/readyz" | jq -e '.ready==true'
      - name: Notify on failure
        if: ${{ failure() }}
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner, repo: context.repo.repo,
              sha: context.payload.deployment_status.sha,
              state: 'failure', context: 'probes',
              description: 'Probe verification failed'
            });
