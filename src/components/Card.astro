---
const { title, cta } = Astro.props;
---
<section class="rounded-xl border border-border bg-panel shadow-brand">
  <div class="flex items-center justify-between border-b border-border p-4">
    <h3 class="text-base font-semibold">{title}</h3>{cta}
  </div>
  <div class="p-4">
    <slot />
  </div>
</section>
