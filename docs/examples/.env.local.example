# Zeropoint Protocol - Local Development Environment

# Local mode flag
LOCAL_MODE=1

# Database (SQLite for local development)
DATABASE_URL="file:./dev.db"

# Feature flags (for local development, mocks can be enabled)
MOCKS_DISABLED=0

# Node environment
NODE_ENV=development

# Server configuration
PORT=3000

# Optional: External service URLs (for production)
# TINYGRAD_API_URL=https://tinygrad.zeropointprotocol.ai
# PETALS_API_URL=https://petals.zeropointprotocol.ai
# WONDERCRAFT_API_URL=https://wondercraft.zeropointprotocol.ai
